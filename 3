#include "stdafx.h"
#include <iostream>
using namespace std;
int main()
{
	float n,x;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		for (int j = 0, l = i,x=1; j < i; j++, l--, x = x * l / j) {
			cout << x<< "\t";
		}
		cout << endl;
	}
}
