#include "stdafx.h"
#include <iostream>
using namespace std;
int main()
{
	float xn=0,xc=1,q,x=3;
	cin >> q;
	for (int i = 0;x > 2.71;i++) {
		xn = 0.5*(xc + q / xc);
		x = pow((xn - xc), 2);
		xc = xn;
	}
	cout << xn << " "<<sqrt(q) << endl;
	return 0;
}
